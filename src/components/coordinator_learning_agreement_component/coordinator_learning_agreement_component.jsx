import React from "react";
import { useNavigate } from "react-router-dom";

export default function CoordinatorLAComponent() {
    const navigate = useNavigate();

    return <div>Coordinator LA</div>;
}
